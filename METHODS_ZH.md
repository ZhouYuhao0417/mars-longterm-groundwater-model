# 模型方法、参数含义与论文使用说明

## 1. 空间模型

交互展示和完成运行均沿用原二维有限体积扩散波模型。空间求解器未替换。计算保留：

- DEM 控制的流向与水面坡度；
- 源洼地的蓄水—溢流关系；
- 最低天然鞍部溢流；
- 下游二维传播与开放边界外排；
- 质量守恒检查。

模型不推测或绘制两条沟槽几何。固定原始点位 `75.937180°E, 18.136689°N` 是两条概念沟槽合计出流的等效源。

## 2. 四个界面参数

### Q

`Q(t)` 是两条概念沟槽合计的原始地下水出流率，单位为 m³/s。该总流量在等效源处只施加一次，不乘源网格数量。

### C

`C` 是沿程有效保留系数：

\[
V_{\mathrm{eff}}(t)=C\int_0^tQ(s)\,ds
\]

\[
V_{\mathrm{loss}}(t)=(1-C)\int_0^tQ(s)\,ds
\]

因此，`C` 不是降雨—径流系数。`C=0.4/0.7/1.0` 用于沿程损失敏感性分析。

### 持续时间 T

`T` 是过程线的截止时间。超过 `T` 后不再增加源项。

### 当前时刻

当前时刻只用于读取该时刻的流量、水量账本和淹没状态，不改变过程线的总持续时间。

## 3. 长期过程线

恒定基流：

\[
Q(t)=Q_b
\]

指数衰减：

\[
Q(t)=Q_b+Q_0\exp(-t/\tau)
\]

分阶段脉冲：

\[
Q(t)=Q_b+Q_0m_k,\qquad t/T\in[a_k,b_k)
\]

当前高情景的无量纲脉冲区间和倍率为：

| t/T 区间 | Q0 倍率 |
|---|---:|
| 0.00–0.02 | 1.00 |
| 0.02–0.08 | 0.35 |
| 0.25–0.28 | 0.80 |
| 0.55–0.57 | 0.60 |

各过程线的累计体积采用解析积分，不用逐日矩形求和。

## 4. 时间推进

交互页采用 1、7、30、90 和 180 天的分段时间尺度显示过程线与诊断量。精确二维运行在需要时使用 600 s 表面水动力步长，并通过动态计算窗口、源洼地解析预填充和经过影子计算验证的稳态跳步降低多年运行成本。

高情景完成运行中：

- 显式二维推进：7.2279 年；
- 验证稳态跳步：21.7440 年；
- 影子验证：3 次通过，0 次失败；
- 下游质量误差：约 `−1.58×10⁻⁷`。

## 5. 水量账本

每个时刻满足：

\[
V_{\mathrm{raw}}
=V_{\mathrm{loss}}
+V_{\mathrm{source}}
+V_{\mathrm{surface}}
+V_{\mathrm{boundary}}
+\epsilon
\]

其中：

- `Vraw`：累计原始出水量；
- `Vloss`：乘 `1−C` 后的沿程损失；
- `Vsource`：源洼地蓄水；
- `Vsurface`：计算域内地表水；
- `Vboundary`：开放边界外排；
- `ε`：数值闭合误差。

开放边界外排使新增水量离开计算域，而不是按累计体积无限扩大淹没范围。

## 6. 完成运行

| 指标 | 低情景 | 高情景 |
|---|---:|---:|
| 持续时间 | 10 年 | 30 年 |
| 原始出水 | 31.5576 km³ | 837.8543 km³ |
| 有效水量 | 12.6230 km³ | 837.8543 km³ |
| 沿程损失 | 18.9346 km³ | 0 km³ |
| 源洼地蓄水 | 12.6230 km³ | 134.5324 km³ |
| 域内地表水 | 0 km³ | 30.8539 km³ |
| 开放边界外排 | 0 km³ | 672.4681 km³ |
| 最大水深 | 115.1065 m | 821.8238 m |
| CRISM 点覆盖 | 0/3 | 0/3 |
| 完成状态 | 完成、论文可用 | 完成、论文可用 |

中情景二维运行只推进到约 3.728 年，`complete=false`、`paper_usable=false`，不能作为论文定量空间结果。

## 7. 旧参数为何造成大面积洪水

旧参数 `160,000 m³/s × 180 d` 的原始累计水量为：

\[
2488.3\ \mathrm{km^3}
\]

乘 `C=0.60` 后仍有约：

\[
1493.0\ \mathrm{km^3}
\]

这约为当前源洼地库容的 11.1 倍。持续超大流量很快填满源洼地并产生大量下游传播和边界外排，因此大面积洪水是参数量级造成的，不是长期地下水基流的合理表现。

## 8. CRISM 点

| 点位 | 解释 | 经度 | 纬度 |
|---|---|---:|---:|
| C1 | Mg-carbonate | 76.632167°E | 17.842999°N |
| P1 | Fe/Mg phyllosilicate | 76.636570°E | 17.807759°N |
| A1 | Al-OH phase (tentative) | 76.545992°E | 17.889783°N |

## 9. 论文使用

建议把 `交互面板_论文截图_中文.tif` 用作方法或补充材料界面图，把 `论文图_完成运行对比_中文.tif` 用作结果图。英文稿使用相应英文 TIFF。

论文正文中的所有数值应从 `data/completed-runs/low_summary.json` 和 `high_summary.json` 引用。交互页只用于方案展示、读者探索和 GitHub Pages 演示。
