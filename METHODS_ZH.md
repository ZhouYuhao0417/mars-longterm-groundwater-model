# 模型方法、参数含义与论文使用说明

## 1. 空间模型

交互展示和完成运行均沿用原二维有限体积扩散波模型。空间求解器未替换。计算保留：

- DEM 控制的流向与水面坡度；
- 源洼地的蓄水—溢流关系；
- 最低天然鞍部溢流；
- 下游二维传播与开放边界外排；
- 质量守恒检查。

固定点位 `75.937180°E, 18.136689°N` 是源坑蓄满后在候选低坑缘出口处施加的单一坑侧规定等效源。它不代表沿东北—西南向 Nili Fossae 沟槽的地表输水，也不预设水能沿该沟槽到达 Jezero 西三角洲流域上游。

## 2. 四个界面参数

### Q

`Q(t)` 是单一坑侧等效输入的规定原始出流率，单位为 m³/s。该总流量只施加一次，不乘源网格数量。它是集总边界条件，不是沿沟槽解析的地表流计算。

### C

`C` 是沿程有效保留系数：

\[
V_{\mathrm{eff}}(t)=C\int_0^tQ(s)\,ds
\]

\[
V_{\mathrm{loss}}(t)=(1-C)\int_0^tQ(s)\,ds
\]

因此，`C` 不是降雨—径流系数。`C=0.4/0.7/1.0` 用于沿程损失敏感性分析。

### 持续时间 T

`T` 是过程线的截止时间。超过 `T` 后不再增加源项。

### 当前时刻

当前时刻只用于读取该时刻的流量、水量账本和淹没状态，不改变过程线的总持续时间。

## 3. 长期过程线

恒定基流：

\[
Q(t)=Q_b
\]

指数衰减：

\[
Q(t)=Q_b+Q_0\exp(-t/\tau)
\]

分阶段脉冲：

\[
Q(t)=Q_b+Q_0m_k,\qquad t/T\in[a_k,b_k)
\]

当前高情景的无量纲脉冲区间和倍率为：

| t/T 区间 | Q0 倍率 |
|---|---:|
| 0.00–0.02 | 1.00 |
| 0.02–0.08 | 0.35 |
| 0.25–0.28 | 0.80 |
| 0.55–0.57 | 0.60 |

各过程线的累计体积采用解析积分，不用逐日矩形求和。

## 4. 时间推进

参数探索器采用 1、7、30、90 和 180 天的分段时间尺度显示过程线与诊断量。已验收中情景先解析预填充 2.660227 年，再以 float64 状态/通量和 600 s 步长完成 17.339773 年显式二维路由，未使用任何下游跳时。旧高情景使用了加速跳时，因此只作为旧版探索结果，不与当前 v2 低/中情景并列为论文结果。

## 5. 水量账本

每个时刻满足：

\[
V_{\mathrm{raw}}
=V_{\mathrm{loss}}
+V_{\mathrm{source}}
+V_{\mathrm{surface}}
+V_{\mathrm{boundary}}
+\epsilon
\]

其中：

- `Vraw`：累计原始出水量；
- `Vloss`：乘 `1−C` 后的沿程损失；
- `Vsource`：源洼地蓄水；
- `Vsurface`：计算域内地表水；
- `Vboundary`：开放边界外排；
- `ε`：数值闭合误差。

开放边界外排使新增水量离开计算域，而不是按累计体积无限扩大淹没范围。

## 6. 已验收 v2 运行

| 指标 | 低情景 | 中情景 |
|---|---:|---:|
| 持续时间 | 10 年 | 20 年 |
| 源坑蓄满／显式路由 | 未溢流／0 年 | 2.660227 年／17.339773 年 |
| 原始出水 | 31.5576 km³ | 473.0025 km³ |
| 有效水量 | 12.6230 km³ | 331.1018 km³ |
| 沿程损失 | 18.9346 km³ | 141.9008 km³ |
| 源洼地蓄水 | 12.6230 km³ | 134.5324 km³ |
| 域内地表水 | 0 km³ | 19.7960 km³ |
| 开放边界外排 | 0 km³ | 176.7734 km³ |
| 最大水深 | 115.1065 m | 380.0000 m |
| HW1–HW3 精确落入 >0.05 m 网格 | 0/3 | 3/3 |
| 完成状态 | v2 已验收 | v2 已验收 |

两者均为 `complete=true`、`paper_usable=true`、全部论文门控通过、水账闭合且无下游跳时。旧的 3.727989 年中情景断点仅作为审计历史保留在 `data/excluded/`，不进入已验收页面。

## 7. 旧参数为何造成大面积洪水

旧参数 `160,000 m³/s × 180 d` 的原始累计水量为：

\[
2488.3\ \mathrm{km^3}
\]

乘 `C=0.60` 后仍有约：

\[
1493.0\ \mathrm{km^3}
\]

这约为当前源洼地库容的 11.1 倍。持续超大流量很快填满源洼地并产生大量下游传播和边界外排，因此大面积洪水是参数量级造成的，不是长期地下水基流的合理表现。

## 8. CRISM 点

| 点位 | 解释 | 经度 | 纬度 |
|---|---|---:|---:|
| HW1 | Fe/Mg-phyllosilicate-like | 76.552282°E | 17.822341°N |
| HW2 | Al-OH-bearing tentative | 76.640344°E | 17.751862°N |
| HW3 | Mg-carbonate-like | 76.681230°E | 17.727558°N |

HW1–HW3 是利用已验收中情景湿润掩膜筛选出的精确含水网格点，因此它们不能作为独立水文验证或矿物物源证明。原 C1/P1/A1 仅保留为补充材料中的预模型负面审计。

## 9. 论文使用

已验收页面用于检查论文图 6(a,b) 背后的低/中情景数组；参数探索器只用于方案展示、读者探索和公开演示。论文正文中的所有定量值必须来自权威 v2 `.npy` 数组和摘要，`data/accepted-v2/` 中的 `.f32` 仅为带哈希的浏览器显示副本。
